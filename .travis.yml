sudo: false
dist: precise
language: python
python: 2.7
os: linux

addons:
  apt:
    packages:
    - graphviz

install: echo install
script: echo foo
cache:
  apt: true
  directories:
    - "$HOME/.cache/pip"
group: stable

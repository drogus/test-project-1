sudo: required
language: ruby
dist: trusty
os:
  - linux

matrix:
  include:
    - rvm: 2.0.0
      env: DB=mariadb55
      addons:
         mariadb: 5.5
         hosts:
           - mysql2gem.example.com
